{
  "name": "frontend-developer-proxy",
  "version": "0.0.1",
  "main": "app.js",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^6.2.0",
    "express": "^4.17.1",
    "fs": "^0.0.1-security",
    "http-proxy-middleware": "^1.0.5",
    "https": "^1.0.0",
    "morgan": "^1.10.0"
  },
  "scripts": {
    "dev": "node app.js",
    "start": "concurrently --kill-others \"npm run dev\" \"cd front && npm start\"",
    "build": "cd front && yarn && yarn run build",
    "preinstall": "cd front && yarn && cd .."
  }
}
